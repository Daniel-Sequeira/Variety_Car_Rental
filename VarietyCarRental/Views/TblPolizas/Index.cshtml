@model IEnumerable<VarietyCarRental.Models.TblPoliza>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionPoliza)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CostoPoliza)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionPoliza)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CostoPoliza)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPoliza">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPoliza">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPoliza">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
